202104197 박서준 

q: 데이터 정렬프로그램에 대해 5가지 종류를 배웠는데 수행시간이 차이가 나는 이유는 뭐야?
a: 알고리즘을 몇번 반복하는지에 따라서 달라지기도 하고, 비교하는 비교군에 차이에 따라 시간복잡도가 차이가 있을수 있어.

q: 버블정렬에 대해서 이해했어?
a: 버블정렬은 이해하기 가장 쉬운 정렬인데 가장앞에 기준을 잡고 뒤에 있는 모든 원소를 비교해서 앞으로 끌고오는 방식이야

q:이번 시간에 데이터 정렬 프로그램 코드를 배웠는데
void Bubble_Sort(int *a, int count)
{
int i, j;
int temp;
for(i = 0; i < count-1; i+ +) // 패스 반복
{
for( j = 1; j < count-i; j+ +) // 최솟값 찾기
{
if(a[j-1] > a[j])
{
temp = a[j-1];
a[j-1] = a[j];
a[j] = temp;
        }
   }
  }
} 여기서 Bubble sort 알고리즘이 어떻게 작용해?

a:Bubble Sort 알고리즘은 배열의 요소를 반복적으로 확인하며 인접한 요소를 비교하고 교환하여 가장 큰 요소를 올바른 위치로 이동시켜. 
이 프로세스는 배열의 모든 요소가 정렬될 때까지 계속된다고 볼수있지.



q:이건 우리가 배운 삽입 정렬 함수 소스인데 
void Insert_Sort(int *a, int count)
{
int i, j;
int temp;
for(i = 1; i < count; i+ +) // 전체 루프 반복
{
temp = a[i];
j = i;
while((a[j-1] > temp) && ( j > 0)) // 원소 삽입 위치 찾기
{
a[j] = a[j-1];
j = j-1;
}
a[j] = temp;
}
} 여기서 왜 'for'루프가 'i=1'부터 시작하는거야?

a:삽입 정렬은 첫 번째 요소를 이미 정렬된 부분 배열로 간주하고 시작해.
따라서 i = 1부터 시작하여 배열의 나머지 부분을 순회하며 각 요소를 정렬된 부분 배열에 삽입하는거지.

q:그렇다면 while 루프의 목적은 뭔데?
a: while 루프는 현재 삽입하려는 요소를 정렬된 부분 배열에서 적절한 위치로 찾아가는 역할을 해. 
정렬된 부분 배열에서 현재 요소보다 큰 요소를 찾으면 그 요소를 한 칸씩 뒤로 이동시키고, 적절한 위치를 찾을 때까지 반복해.

q: 변수 'temp'의 역할은 뭔데?
a:temp 변수는 현재 비교 대상인 요소를 임시로 저장하는 데 사용 돼. 이 변수를 통해 현재 요소와 비교 대상을 교환하며 정렬을 수행하는거지.
